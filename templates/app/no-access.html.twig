{% extends 'app/base.html.twig' %}

{% block page %}
    {{ parent() }}
{% endblock %}

{% block content %}
    <div class="header">
        <h1>{{ 'No access'|trans }}</h1>
        <p class="lead">{{ 'Your app is not active'|trans }}</p>
    </div>
    <div class="text-center">
        <i class="fa-solid fa-ban fa-5x"></i>
        <p class="mt-4">{{ 'If you think this is an error please contact you guest access administrator at Dokk1 to have your guest access app enabled'|trans }}</p>
    </div>
{% endblock %}
