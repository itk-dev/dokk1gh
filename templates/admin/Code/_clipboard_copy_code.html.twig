<span class="code-identifier">
  <kbd>{{ code.identifier }}</kbd>
  <button class="btn btn-xs"
          type="button"
          data-clipboard-text="{{ code.identifier }}"
          data-clipboard-success="{{ 'Code copied to clipboard'|trans }}">
    <span class="fa fa-clipboard"></span>
  </button>
</span>
