{% extends ':Emails:layout.html.twig' %}

{% block email_title %}{{ include(template_from_string(password_resetting.header)) }}{% endblock %}

{% block header %}
  {{ include(template_from_string(password_resetting.header)) }}
{% endblock %}

{% block body %}
  {{ include(template_from_string(password_resetting.body)) }}
{% endblock %}

{% set button = {
  url: confirmationUrl,
  text: password_resetting.button.text
} %}

{% block footer %}
  {{ include(template_from_string(password_resetting.footer)) }}
{% endblock %}
